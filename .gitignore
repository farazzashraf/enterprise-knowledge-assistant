# ── Virtual environment ──────────────────────────────────────
venv/
env/
ENV/

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# ── Secrets / environment ────────────────────────────────────
# NEVER commit the real .env (contains API keys). .env.example IS committed.
.env
.env.local
.env.*.local

# ── IDE / OS ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Testing ──────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/

# ── Runtime / generated artifacts ────────────────────────────
qdrant_storage/            # local Qdrant docker volume
.streamlit/
*.log
logs/
models/
*.pkl                      # BM25 index + corpus (regenerated by ingestion)
*.pickle
*.joblib
data/embeddings/
data/eval_results.json     # regenerated by the eval harness
data/feedback.jsonl        # runtime user feedback

# ── Assignment brief (not part of the deliverable) ───────────
/*.pdf                     # root-level PDFs only; data/sample_documents/*.pdf are kept

# ── Redundant/working docs (consolidated into SYSTEM_ARCHITECTURE.md) ──
/TECHNICAL_DOCUMENTATION.md
/project_documentation.md
/PLAN.md
/VALIDATION.md
